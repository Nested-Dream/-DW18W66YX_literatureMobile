import React from "react";
import { Text, View, TouchableOpacity } from "react-native";
import { ContentCenter } from "../../styles/StyledComponents";
const Home = (props) => {
  return (
    <ContentCenter>
      <Text>Di sini Home</Text>
    </ContentCenter>
  );
};

export default Home;
