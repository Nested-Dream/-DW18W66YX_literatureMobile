import React from "react";
import { Text, View, TouchableOpacity } from "react-native";
import { ContentCenter } from "../../styles/StyledComponents";
const Search = (props) => {
  return (
    <ContentCenter>
      <Text>Di sini Search</Text>
    </ContentCenter>
  );
};

export default Search;
