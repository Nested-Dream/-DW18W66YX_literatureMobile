import React from "react";
import { Text, View, TouchableOpacity } from "react-native";
import { ContentCenter } from "../../styles/StyledComponents";
const Profile = (props) => {
  return (
    <ContentCenter>
      <Text>Di sini Profile</Text>
    </ContentCenter>
  );
};

export default Profile;
